{
  "name": "seotracker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare": "node scripts/setup-git-hooks.mjs",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "NODE_OPTIONS=--experimental-strip-types ultracite check",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "NODE_OPTIONS=--experimental-strip-types oxfmt --write .",
    "format:check": "NODE_OPTIONS=--experimental-strip-types oxfmt --check .",
    "check": "NODE_OPTIONS=--experimental-strip-types ultracite check",
    "verify": "pnpm format:check && pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "ci:format": "pnpm format:check",
    "ci:lint": "NODE_OPTIONS=--experimental-strip-types oxlint -c oxlint.config.ts .",
    "ci:typecheck": "pnpm typecheck",
    "ci:test": "pnpm test",
    "ci:build": "pnpm build",
    "ci:audit": "pnpm audit --prod --audit-level moderate",
    "ci:e2e": "pnpm --filter @seotracker/shared-types build && pnpm --filter @seotracker/server build && pnpm test:e2e",
    "db:generate": "pnpm --filter api db:generate",
    "db:migrate": "pnpm --filter api db:migrate",
    "db:studio": "pnpm --filter api db:studio",
    "test:e2e": "pnpm --filter api test:e2e",
    "fix": "NODE_OPTIONS=--experimental-strip-types ultracite fix"
  },
  "devDependencies": {
    "oxfmt": "0.48.0",
    "oxlint": "1.63.0",
    "simple-git-hooks": "2.13.1",
    "turbo": "^2.9.9",
    "typescript": "^6.0.3",
    "ultracite": "7.6.3"
  },
  "simple-git-hooks": {
    "pre-commit": "pnpm format:check && pnpm lint",
    "pre-push": "pnpm verify"
  },
  "packageManager": "pnpm@11.0.8"
}
